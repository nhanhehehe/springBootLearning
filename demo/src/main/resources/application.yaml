server:
  port: 8080
  servlet:
    context-path: /identity

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/identity"
    username: root
    password: my-secret-pw

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true