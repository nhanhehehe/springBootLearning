repo:
extends jpaRepo<..., ...>
jpa sẽ làm gì

multi-layer: +mỗi request đi qua 3 lớp: controller (qly mapping endpoint), service(logic), repo(ttac database(kh chỉ dbms, lưu trong file hoặc hệ thống khác)
             +mỗi layer chỉ được gọi layer dưới nó

service:
mapping các data từ quest vào object sử dụng annotation là @RequestedBody


jpa:
hibernate:
  ddl-auto: update
show-sql: true
-> thay vì viết scirpt database thì jpa cách này tự generate bảng vào database cho thuận lợi mục đích học


@GetMapping("/{userId}")
    User getUser(@PathVariable String userId) {

    }
->để map thông tin của một biến từ path dùng trong dấu {} và dùng annotation PathVariable để mapping biến trên path vào tham số ví dụ GetMapping("{/userId}") -> (@PathVariable String userId)


@PutMapping và @RequestedBody: mapping dữ liệu trong request vào dto

=> buoi #2: tao api crud, cau hinh connect dbms(Database Management System – tức là Hệ quản trị cơ sở dữ liệu), tao crud tuan theo layer architecture: 3 layer: controller - service - repo

---------------------------------------------------------------------------------------------------

- quản lý exception trong spring, có annotation @ControllerAdvice. dùng để define các loại exception (bắt buộc có trong mọi dự án)

-ResponseEntity<String> là gì

- taị sao hàm tìm userName lại làm bằng cách tạo hàm tìm bằng  id trong repo sau đó đổi tên thành UserName thì nó tự config để tìm theo username ?

-lỗi từ người dùng là 4xx -> badRequest()

-dùng validation spring cần add thêm dependency

- 2026-04-25T10:19:55.719+07:00  WARN 12472 --- [nio-8080-exec-4] .w.s.m.s.DefaultHandlerExceptionResolver :
Resolved [org.springframework.web.bind.MethodArgumentNotValidException:
Validation failed for argument [0] in com.example.demo.entity.User com.example.demo.controller.UserController.createUser(com.example.demo.dto.request.UserCreationRequest):
[Field error in object 'userCreationRequest' on field 'password': rejected value [1234]; codes [Size.userCreationRequest.password,Size.password,Size.java.lang.String,Size];
arguments [org.springframework.context.support.DefaultMessageSourceResolvable: codes [userCreationRequest.password,password]; arguments []; default message [password],2147483647,8];
default message [password must be at least 8 characters]] ]
    -> lỗi như này tức trong globalHandler phải thêm @ExceptionHandler(value = tên_loại_exception)

- .getFieldError(): field do la gi? field luc user nhap vao la gi
    -> vi du: .getFieldError().getDefaultMessage()

-mot so annotation cho viec validation: email, notBlank, notNull,... hoac custome validation annotation

=> buoi #3: cau hinh va handle cac exception va quan ly validation
--------------------------------------------------------------------------------------------------------------------------
exception handling nâng cao và normalize api response ( chuan hoa api response -> client hieu duoc return thanh cong hoac return loi :vd lỗi 404, 202, ...)
https://docs.apilayer.com/currencylayer/docs/getting-started
-> mọi api response cần co mot structor để chung một format => SINH RA ApiResponse

code:1000-> success

@JsonInclude(JsonInclude.Include.NON_NULL) -> khi parse object -> json: nếu null, sẽ không hiện trong json
    -> giúp clean, tăng performance, giảm confused cho client, giống trong production thực tế


để chuẩn hóa các api response -> tạo riêng Enum để xác định loại error cụ thể và AppException để define lại những error từ Enum

để handle riêng exception mà mình không lường trc được -> tạo enum UNCATEGORIZED_ERROR và xử lý nó với exception.class để giúp xác định tường minh hơn

đối với lỗi validation MethodArgumentNotValidException, khi xác định enum sẽ khác hơn
    trong dto, bình thường @valid gồm 2 tham số(size, message) -> thay message ở đây thành chuỗi String của enum cần define(ví dụ ở đây là USERNAME_INVALID)
    trong global, dùng ErrorCode.valueOf(enumKey) để biến từ chuỗi -> enum.
    Còn lại y chang những handle exception khác với enum và exception
*=> có rủi ro nếu trong tham số message của @Valid viết sai chính tả -> hệ thống k define được lỗi -> Solution: tạo một errorCode enum riêng để xử lý bug này giống với UNCATEGORIZED_ERROR

ErrorCode errorCode = ErrorCode.KEY_VALID;


        try {
            errorCode = ErrorCode.valueOf(enumKey);
        } catch (IllegalArgumentException e) {}

=> * ở đây khi valueOf(enumKey) X -> lập tức throw -> nên errorCode vẫn sẽ không gán nên vẫn là có giá trị là ErrorCode.KEY_VALUE


=> buoi #4 normalize các api response ( code, message, và result(kiểu dữ liệu trả về) thay vì chỉ trả về api response dữ dạng một object gồm chuỗi thuần -> thông qua một dto ApiResponse
xử lý các error trong validation dưới dạng normalize api response, xử lý chung các error unexpected -> Tất cả đều thông qua Enum và một exception handle riêng cho các lỗi

-----------------------------------------------------------------------------------------------

lombok: getter và setter
mapstruct: mapping các dto

các getter và setter do lombok sẽ được define trong mục target

Annotaton @Data cung cấp các annotation khác như Getter, Setter, RequiredArgsConstructor j đó, ToString, EqualsAndHashCode
Annotation @Builder(dùng để tạo object theo Builder Pattern) cung cấp cách tạo object tường minh hơn vì gọi thuộc tính và bỏ giá trị vào tham số -> tên_class.Builder().tên_thuộc_tính(value)...
    *-> dùng builder -> lỗi khi tạo object kh tham số

AllArgsConstructor và NoArgsConstructor

@fieldDefaults cung cấp access modifier mặc định trong class

@Mapper(componentModel = "spring") giúp mapstruct biết generate mapper trong spring theo kiểu Dependency injection

có 2 kiểu mapper: có kiểu dữ liệu và void:
    User toUser(UserCreationRequest reuqest);
    void updateUser(@MappingTarget User user, UserUpdateRequest request); @MappingTarget -> mapping request tới user


dữ liệu khi trả về phải là dto thay vì là entity

Trong mapstruct còn có annotation giúp mapping dễ dàng đó là: @Mapping(source="", target="") -> giúp cho đồng bộ kiểu dữ liệu
    vd: source="firstName" target="lastName" -> firstname sẽ giống lastName
    *ngoài ra thêm tham số trong mapping() là ignore -> không mapping thuộc tính define trong source=""


*hot take: @autowired không best pratical để inject bean -> dùng construct để inject và lombok có hỗ trợ
    -> @RequiredArgsConstructor -> tạo constructor cho các biến final -> tự inject dependency kết hợp @DefaultField(level là private, makeFine là true)

buoi #5=> lombok vaf mapstruct, autowired -> RequiredArgsConstructors, các Annotation khi dùng mapstruct, @RequiredArgsConstructor vaf  @FieldDefaults(level = AccessLevel.PRIVATE, makeFinal = true)
-------------------------------------------------------------------------------------------------

khi trả về dữ liệu cho client, phải mã hóa password tránh password bị hackler phát hiện

vấn đề: mã hóa theo 2 chiều: 1 key để mã hóa và key để xem -> hacker vẫn biết
    ->solution: hash mật khẩu (md5 kém bải mật -> dùng "bcrypt")
    Hash: key ở việc bảo mật là dùng phép toán modulo để kh truy vết ngược lại: a % b = c -> c không thể truy vết ngược lại a và b
        -> drawback: có thể dùng rainbow hack
        -> vẫn phổ biến bây giờ nhưng không bảo mật tuyệt đối về việc mã hóa dữ liệu

bcrypt: tạo random salt -> mã hóa salt và password

PasswordEncoder passwordEncoder = new BCryptPasswordEncoder(10); -> truyền tham số là độ phức tạp của mã hóa
user.setPassword(passwordEncoder.encode(user.getPassword())); -> method encode mã hóa password

Optional<User> findByUsername(String username); ??? optional la gì

normally: chỉ truyền username và password 1 lần để mã hóa -> tạo token để lần khác sử dụng thay vì truyền username và password nhiều lần
    -> bảo vệ mật khẩu user an toàn

=> #buoi6: sử dụng builder cho trả về AuthResponse, Bscrypt, cơ chế hash bằng modulo
--------------------------------------------------------------------------------------------------------


JWT, token

token base authentication ra đời để hỗ trợ việc xác thực nhanh gọn bớt rườm rà

JWT: header, payload, signature.
    -> payload k nên chứa thông tin quá nhạy cảm, chỉ nên gồm như: obj, role, id, ...
    signature là hash của header + payload + salt secret

    salt: ký (sign) và xác minh (verify) token.
        -> kết hợp với secret key để tạo ra signature bằng thuật toán như HMAC SHA256 (giúp token ko bị sửa, token đc tạo hợp lệ bởi server)
        ->Khi client gửi JWT lên server: - Server sẽ dùng lại secret key đó, - Tự tính lại signature từ header + payload, - So sánh với signature trong token

-để tạo, kí một token dùng thư viện nimbusd:

JWSObject: gồm header, payloads

header: chua thong tin thuat toan

data trong body goi la claim (claim set) -> noi dung duoc goi di trong token: subject, userId, ... nhwng thong tin essential

-> tao payload - > dùng JWTClaimsSet de tao data ben trong payload, sau do chuyen sang json va truyen vao param cua payload constructor

-> claim subject:dai dien cho user dang dang nhap

->issuer ddeer bt token duoc issue tu user nao ( thuong la ten domian, ...)

=> từ header và payload -> tạo json web signature object để kí(sign) -> serialize


-verify token:


- có thể giả mạo token nếu có đc secret key -> giấy key trong file khác thay vì code thẳng

@Value(${jwt.}) -> giúp đọc value từ file yaml

tai sao viet: protected static final String SIGNER_KEY; -> protected String SIGNER_KEY



=> buoi #7: tao token, ki token de xac thuc noi dung khong bi thay doi; token valid: nội dung không thay đổi và kh expired, verify token
-------------------------------------------------------------------------------------------------


spring security -> để validate token; là một hệ thống xây dựng dựa trên kiến trúc filter của một servlet

spring security là một cấu trúc dùng để xác thực thông tin validate trước khi tới lớp controller

thứ tự của từng filter rất quan trọng; đặc tính filter: có thể cho request deny hoặc accept tới filter khác hoặc trả về client

servlet chỉ thấy 1 filter của spring security NHƯNG một filter security gồm nhiều filter

trước khi client đến controller: hệ thống security như sau:
    client <-> filter <-> FilterChainProxy <-> filter <-> servlet <-> controller, repo, service

aouth2 config security spring

config security: define những endpoint nào cần bảo vệ và endpoint nào user có thể access (get, ...)
    - public: ai cx có thể truy cập (ví dụ như register, login...) -> vì lúc này ng dùng ch có credential nên kh thể vào
        -> class HttpSecurity cho phép làm điều đó through những method:
            -> class HttpSecurity cung cap method authorizeHttpRequest, include lamda function
               that include matchers function that include 2 param: + name of method, + name of endpoint
               , permitAll -> anyRequest -> authenticated
                    ->aim: cho phép public các endpoint
            *hot take: chỉ public nhưng kh được truy cậo vì mặc định spring.csrf() (bao vệ endpoint khỏi attack cross high) -> chưa hiện nội dung
                -> httpSecurity.csrf(AbstractHttpConfigurer::disable); //AbstractHttpConfigurer -> AbstractHttpConfigurer.disable()

        -> cho phép authorization bear với token thông qua outh2ResourceServer
        , chứa lamda oauth2.jwt, chứa jwtConfigurer.decoder() -> authenticate jwt mà không ngoài idb thứ 3 nào
                *decoder là một interface nên phải implement lại
                    -> sử dụng nimbus (dependency oauth2 chứa dependency nimbus -> xóa dependency nimbus
                    -> implement: trả về nimbusjwtdecoder gồm 2 spec(withsecretkey và macalgorithm): +secret key và +algorithm
                        + secretkey: tạo một biến chứa secret key và thuật toán hash
                        + algo: chứa thuật toán

flow:
httpSecurity.oauth2ResourceServer(oauth2 ->
                oauth2.jwt(jwtConfigurer -> jwtConfigurer.decoder(jwtDecoder()))
        );
muốn đăng ký một auth provider manager để support jwt token

=>#buoi8 sử dụng spring security để authencate với jwt; architect của spring security, ...

----------------------------------------------------------------------------------------------

userResponse k chứa mật khẩu

token valid chưa đủ -> không phải cũng có quyền làm công việc abc -> authorization

*khi update những dto mà có dùng mapstruct thì nên clean lifecycle trong maven

admin chỉ được tạo một lần duy nhất khi install chương trình hoặc initial chương tình -> tạo một config
    ApplicationRunner là một Bean, chỉ chạy khi init chương trình, trả về môn hàm lamda arg
        -> dùng để truyền role Admin


BScript sử dụng nhiều nơi nên tạo riêng config


câu hỏi phụ: tại sao dùng builder, (Role.ADMIN.name());, tìm admin bằng userRepository.findByUsername("admin").isEmpty(), private String buildScope(User user) {
                                                                                                                                  StringJoiner stringJoiner = new StringJoiner(" ");
                                                                                                                                  if (CollectionUtils.isEmpty(user.getRoles())) {
                                                                                                                                      user.getRoles().forEach(stringJoiner::add);
                                                                                                                                  }
                                                                                                                                  return stringJoiner.toString();
                                                                                                                              }
nhắc bài cũ: khi add @Bean method sẽ được đưa vào application context để lấy sử dụng, tại sao k sử dụng autowired mà dùng allArgsConstr, ...
             @Slf4j // lombok dungf để log,

spring security hỗ trợ Authorization bằng jwt token (chỉ cần config cho đúng)
    khi authorize bằng authentication và authorize bằng token để spring security có thể handle token
        -> role phải có trong token -> hiện tại token chưa có role -> cần thêm

    oauth2 quy định token có claim scope that chứa list các role cách nhau bởi dấu cách
        -> sau khi xác thực token -> hệ thống decode token chứa claim scope để lấy thông tin role và map role đó cho user

        tạo hàm builScope để nối các roles theo dạng cách nhau bởi dấu " " vì roles trong user là một list
            -> cần define về dạng cách nhau bởi dấu cách


    Sau khi đã có claim scope về role, SS sẽ map scope vào authority của user khi đăng nhập thành công
        -> để xác thực thì: trong securityConfig: set requestMatcher get method chỉ admin mởi truy cập được
                    ^
                    |
                    v
để get user được authen trong request -> sử dụng securityContextHolder
    -> có thể lấy ra các authorities về role THAT param trong requestMatcher.hasAuthority(param) hoặc .hasRole("ADMIN") (Role.ADMIN.name())

    => SCOPE_ADMIN chính là param trong hasAuthority của requestMatcher, và nó được lấy từ securityContextHolder
        -> Spring security cung cấp customize, NGOÀI config về decoder RA CUNG CẤP thêm config converter(jwtAuthenticationConverter) nhưng cần overwrite bean lại: @Bean
                                                                                                                                                                      JwtAuthenticationConverter jwtAuthenticationConverter() {
                                                                                                                                                                          JwtGrantedAuthoritiesConverter jwtGrantedAuthoritiesConverter = new JwtGrantedAuthoritiesConverter();
                                                                                                                                                                          jwtGrantedAuthoritiesConverter.setAuthorityPrefix("ROLE_");
                                                                                                                                                                          JwtAuthenticationConverter jwtAuthenticationConverter = new JwtAuthenticationConverter();
                                                                                                                                                                          jwtAuthenticationConverter.setJwtGrantedAuthoritiesConverter(jwtGrantedAuthoritiesConverter);
                                                                                                                                                                          return jwtAuthenticationConverter;
                                                                                                                                                                      }

                                                                                                                                                                      -> sửa prefix thành Role thay vì SCOPE


#buoi9: authorize role admin và user, contextHolder, jwtAuthenticationConverter, hasAuthority (chỉ ai cò quyền mới được) or hasRole, buildScope, .claim("scope", buildScope(user)),
--------------------------------------------------------------------
authorize với jwt, SecurityContextHolder, pre and post Authorize (phana quyền dựa trên method thay vì endpoint)

preAuth: cung cấp 1 proxy mà trước khi đến method này kiểm tra bên trong param
    -> preAuth thay thế cho requestMatcher().hasAuthority() or hasRole()

postAuth: chạy method xong nếu condition đúng thì mới trả về thông tin -> ít ứng dụng ()
    -> chỉ user lấy thông tin của chính mình: @PostAuthorize("returnObject.username == authentication.name")
more: https://docs.spring.io/spring-security/reference/servlet/authorization/method-security.html


method: lấy thông tin user đang đăng nhập là ai
    khi thông tin request được xác thực thành công -> thông tin sẽ đc lưu trong SecurityContextHolder
        -> có thể get để lấy thông tin


=> #buoi 10
----------------------------------------------------------------------------
advanced handling exception và trả về response với http tùy biến
    khi handling exception, một số http response khác nhau trong khi chúng ta mong muốn http tương ứng với các exception tương ứng


HttpStatusCode (interface) -> mong muốn với mỗi errorCode: gồm code, message và http status

INTERNAL_SERVER_ERROR (500): lỗi không xác định được
badRequest() : lỗi liên quan đến input user
NotFound(404) : theo convention thông thường, là kh tìm thấy
unauthrized(401) : Không xác thực được thông tin đăng nhập credentials

trong globalExceptionHandler chúng tra thường dùng .badRequest -> đổi lại get statusCode trong Class enum ErrorCode

unauthenticated: 401
unauthorized: 403

define AccessDeniedException khi get users với role user -> lỗi 403

lỗi 401 không thể xử lý ở global handle mà phải trong security config vì nó nằm ở trong các "tầng filter"
    -> nằm ở trong .authenticationEntryPoint() -> phải implement lại
        thường là sai token, token expired, ...

chưa hiểu: JwtAuthenticationEntryPoint